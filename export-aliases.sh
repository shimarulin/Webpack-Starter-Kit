#!/bin/bash

echo "Export aliases to shell session"
echo "You must use \"source\" command for call this script"
alias npm='docker-compose run npm'
alias webpack='docker-compose run webpack'
alias wds='docker-compose run wds'